{
  "name": "Writing Studio",
  "install": "npm ci && bash scripts/agent-env-setup.sh bootstrap",
  "ports": [
    {
      "name": "Next.js",
      "port": 3000
    }
  ],
  "terminals": [
    {
      "name": "Convex",
      "command": "CONVEX_AGENT_MODE=anonymous npx convex dev --tail-logs disable",
      "description": "Starts an isolated local Convex backend and watches backend functions."
    },
    {
      "name": "Next.js",
      "command": "CONVEX_AGENT_MODE=anonymous bash scripts/agent-env-setup.sh start",
      "description": "Configures Convex runtime secrets after it is ready, then starts Next.js."
    }
  ]
}
